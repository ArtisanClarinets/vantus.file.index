<Page x:Class="Vantus.App.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:Vantus.App.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:DashboardViewModel, IsDesignTimeCreatable=True}"
      Title="Dashboard">

    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="System Status" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <UniformGrid Columns="2" Rows="2">
                <ui:Card Margin="0,0,8,8">
                    <StackPanel Margin="16">
                        <TextBlock Text="Engine Status" Opacity="0.6" />
                        <TextBlock Text="{Binding Status}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    </StackPanel>
                </ui:Card>
                <ui:Card Margin="8,0,0,8">
                    <StackPanel Margin="16">
                        <TextBlock Text="Files Indexed" Opacity="0.6" />
                        <TextBlock Text="{Binding FilesIndexed}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    </StackPanel>
                </ui:Card>
                <ui:Card Margin="0,8,8,0">
                    <StackPanel Margin="16">
                        <TextBlock Text="Total Tags" Opacity="0.6" />
                        <TextBlock Text="{Binding TotalTags}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    </StackPanel>
                </ui:Card>
                <ui:Card Margin="8,8,0,0">
                    <StackPanel Margin="16">
                        <TextBlock Text="Total Partners" Opacity="0.6" />
                        <TextBlock Text="{Binding TotalPartners}" FontSize="24" FontWeight="Bold" Margin="0,8,0,0"/>
                    </StackPanel>
                </ui:Card>
            </UniformGrid>
        </StackPanel>
    </Grid>
</Page>
