<Page x:Class="Vantus.App.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:Vantus.App.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="{Binding Intro}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,24" TextWrapping="Wrap"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Settings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                <TextBlock Text="{Binding Label}" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="{Binding HelperText}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                            </StackPanel>
                            
                            <!-- Control Selector -->
                            <ContentControl Grid.Column="1" Content="{Binding}" VerticalAlignment="Center" HorizontalAlignment="Right">
                                 <ContentControl.Resources>
                                      <!-- Toggle -->
                                      <DataTemplate DataType="{x:Type viewmodels:ToggleSettingViewModel}">
                                          <ui:ToggleSwitch IsChecked="{Binding Value}" />
                                      </DataTemplate>
                                      <!-- Dropdown -->
                                      <DataTemplate DataType="{x:Type viewmodels:DropdownSettingViewModel}">
                                          <ComboBox ItemsSource="{Binding Options}" SelectedItem="{Binding Value}" MinWidth="120"/>
                                      </DataTemplate>
                                      <!-- Slider -->
                                      <DataTemplate DataType="{x:Type viewmodels:SliderSettingViewModel}">
                                          <Slider Value="{Binding Value}" Minimum="{Binding Min}" Maximum="{Binding Max}" TickFrequency="{Binding Step}" Width="200"/>
                                      </DataTemplate>
                                 </ContentControl.Resources>
                            </ContentControl>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
